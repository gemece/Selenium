package tests;

import static utils.extentreports.ExtentTestManager.startTest;

import java.lang.reflect.Method;

import org.testng.annotations.Test;

public class SearchTest {
	
	@Test(priority = 0, description = "Succesfull search hotel in the page")
	public void succesSearchTest(Method method) {
		// ExtentReports Description
		startTest(method.getName(), "Succesfull search hotel in the page.");

		homePage.goToBarceloHotel().goToLoginPage().loginBarcelo("gmuocre@etsinf.upv.es", "acg12345").verifyLogin("Guillermo");
	}

}
